{
  runTest,
  lib,
  pkgs,
  self,
}:
{
  # keep-sorted start case=no
  # espeak-ng-alsa = runTest (import ./espeak-ng-alsa.nix { inherit self pkgs lib; });
  # espeak-ng-libao = runTest (import ./espeak-ng-libao.nix { inherit self pkgs lib; });
  # espeak-ng-oss = runTest (import ./espeak-ng-oss.nix { inherit self pkgs lib; });
  # espeak-ng-pipewire = runTest (import ./espeak-ng-pipewire.nix { inherit self pkgs lib; });
  # espeak-ng-pulse = runTest (import ./espeak-ng-pulse.nix { inherit self pkgs lib; });
  # espeak-ng-mbrola = runTest (import ./espeak-ng-mbrola.nix { inherit self pkgs lib; });
  # festival = runTest (import ./festival.nix { inherit self pkgs lib; });
  # mary = runTest (import ./mary.nix { inherit self pkgs lib; });
  # keep-sorted end
}
